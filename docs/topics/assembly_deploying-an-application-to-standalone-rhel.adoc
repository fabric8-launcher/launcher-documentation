[id='deploying-an-application-to-standalone-rhel_{context}']
= Deploying {runtime} application to standalone Red Hat Enterprise Linux

ifdef::built-for-vertx[]
To deploy an {runtime} application on a standalone Red{nbsp}Hat Enterprise Linux, you must first package the application. You can then deploy it using `java -jar` command. 
endif::[]
ifndef::built-for-vertx[]
To deploy a {runtime} application on a standalone Red{nbsp}Hat Enterprise Linux, you must first package the application. You can then deploy it using `java -jar` command.
endif::[] 

include::proc_preparing-an-application-for-standalone-red-hat-enterprise-linux-deployment.adoc[leveloffset=+1]

include::proc_deploying-an-application-to-standalone-red-hat-enterprise-linux-using-jar.adoc[leveloffset=+1]